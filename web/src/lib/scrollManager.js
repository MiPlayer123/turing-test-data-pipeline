// Scrollama wrapper — placeholder for scroll-triggered animations
// Will be wired up in Phase 3 when we add scroll-driven section transitions
export function initScrollManager() {
  // For now, sections init immediately on data load
  // TODO: lazy-init sections as they enter viewport
}
